export const environment = {
  production: false,
  // Make sure this URL matches where your Laravel backend is running.
  // Default for Laravel local server is http://127.0.0.1:8000
  apiUrl: 'http://127.0.0.1:8000/api'
};
